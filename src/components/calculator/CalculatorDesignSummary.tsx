"use client";

/**
 * Shared Design Summary — re-exports the bearings-shared panel as the fleet standard (EDP-1).
 */
export {
  default,
  type DesignSummaryRow,
  type DesignSummaryRowStatus,
} from "@/components/machine/bearings-shared/ModuleDesignSummaryPanel";
