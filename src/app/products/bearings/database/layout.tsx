import { bearingToolLayout } from "@/lib/machine/bearings/bearingToolLayout";

const { metadata, Layout } = bearingToolLayout("/products/bearings/database");
export { metadata };
export default Layout;
