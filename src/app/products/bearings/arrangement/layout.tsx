import { bearingToolLayout } from "@/lib/machine/bearings/bearingToolLayout";

const { metadata, Layout } = bearingToolLayout("/products/bearings/arrangement");
export { metadata };
export default Layout;
