import { bearingToolLayout } from "@/lib/machine/bearings/bearingToolLayout";

const { metadata, Layout } = bearingToolLayout("/products/bearings/failure");
export { metadata };
export default Layout;
