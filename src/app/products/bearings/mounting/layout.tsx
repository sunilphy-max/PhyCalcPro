import { bearingToolLayout } from "@/lib/machine/bearings/bearingToolLayout";

const { metadata, Layout } = bearingToolLayout("/products/bearings/mounting");
export { metadata };
export default Layout;
